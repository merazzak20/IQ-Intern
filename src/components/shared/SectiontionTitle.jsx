import React from "react";

const SectiontionTitle = ({ title }) => {
  return (
    <div>
      <h2 className="sectionTitle text-4xl font-bold">{title}</h2>
    </div>
  );
};

export default SectiontionTitle;
